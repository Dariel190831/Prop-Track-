import { defineConfig, devices } from '@playwright/test'

// E2E = la cima de la piramide: pocos, pero prueban la app como la usaria
// un humano real (clicks en el navegador), no imports internos como los
// unitarios de vitest. Por eso solo hay un proyecto (chromium) y un solo
// spec por ahora.
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  reporter: process.env.CI ? [['list'], ['html', { open: 'never' }]] : 'list',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
    timeout: 60000,
  },
})
